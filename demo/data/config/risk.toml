# Risk/Greeks configuration (Bump-and-Revalue method)

greeks_method = "bump"
target_greeks = ["delta", "gamma", "vega", "theta", "rho"]
second_order_mode = "parallel"

[bump_sizes]
rate = 0.0001
vol = 0.01
spot = 0.01
