# FrictionalBank デモ仕様書

## 概要

FrictionalBankは、Neutryxライブラリの全機能を活用した仮想銀行システムのデモンストレーションです。A-I-P-Sアーキテクチャの正しいデータフローに沿って、外部システムとの連携を含めた統合的なデモを提供します。

## 正しいデータフロー

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│                          外部システム（入力元）                               │
│  [Reuters] [Bloomberg] [FpML取引システム] [CSV/Parquet] [設定ファイル]        │
└──────────────────────────────┬──────────────────────────────────────────────┘
                               ↓
┌──────────────────────────────┴──────────────────────────────────────────────┐
│  A: Adapter Layer                                                            │
│  adapter_feeds (マーケットデータ受信)                                         │
│  adapter_fpml (FpML取引受信)                                                 │
│  adapter_loader (CSV/ファイル受信)                                           │
└──────────────────────────────┬──────────────────────────────────────────────┘
                               ↓
┌──────────────────────────────┴──────────────────────────────────────────────┐
│  I: Infra Layer (横断的基盤)                                                 │
│  infra_config (設定読込)                                                     │
│  infra_master (カレンダー、通貨マスタ)                                        │
│  infra_store (永続化・状態管理)                                              │
└──────────────────────────────┬──────────────────────────────────────────────┘
                               ↓
┌──────────────────────────────┴──────────────────────────────────────────────┐
│  P: Pricer Layer (計算カーネル)                                              │
│  pricer_core → pricer_models → pricer_optimiser → pricer_pricing → pricer_risk│
└──────────────────────────────┬──────────────────────────────────────────────┘
                               ↓
┌──────────────────────────────┴──────────────────────────────────────────────┐
│  S: Service Layer                                                            │
│  service_cli (バッチ結果出力)                                                │
│  service_gateway (REST/gRPC出力)                                            │
│  service_python (Python連携出力)                                             │
└──────────────────────────────┬──────────────────────────────────────────────┘
                               ↓
┌──────────────────────────────┴──────────────────────────────────────────────┐
│                          外部システム（出力先）                               │
│  [規制当局API] [決済システム] [リスクダッシュボード] [Jupyter] [CSV出力]        │
└─────────────────────────────────────────────────────────────────────────────┘
```

## フォルダ構成

```text
neutryx-rust/
├── demo/
│   │
│   ├── inputs/                        # ⬆️ 入力システム（Adapterへの入力元）
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── lib.rs
│   │       ├── market_data_provider/  # マーケットデータ配信元
│   │       │   ├── mod.rs
│   │       │   ├── reuters_sim.rs     # Reuters風ティッカー配信
│   │       │   ├── bloomberg_sim.rs   # Bloomberg風スナップショット配信
│   │       │   └── synthetic.rs       # 合成データ生成器
│   │       ├── trade_source/          # 取引データ発生源
│   │       │   ├── mod.rs
│   │       │   ├── front_office.rs    # フロントオフィスブッキング
│   │       │   └── fpml_generator.rs  # FpML取引生成
│   │       └── file_source/           # ファイル生成元
│   │           ├── mod.rs
│   │           ├── csv_generator.rs   # CSV生成
│   │           └── parquet_writer.rs  # Parquet生成
│   │
│   ├── outputs/                       # ⬇️ 出力システム（Serviceからの出力先）
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── lib.rs
│   │       ├── regulatory/            # 規制当局システム
│   │       │   ├── mod.rs
│   │       │   ├── regulator_api.rs   # 規制報告受付API
│   │       │   └── audit_store.rs     # 監査証跡ストア
│   │       ├── settlement/            # 決済システム
│   │       │   ├── mod.rs
│   │       │   ├── swift_receiver.rs  # SWIFT風メッセージ受信
│   │       │   └── netting_engine.rs  # ネッティング処理
│   │       ├── risk_dashboard/        # リスクダッシュボード
│   │       │   ├── mod.rs
│   │       │   ├── websocket_sink.rs  # WebSocket受信サーバー
│   │       │   └── metrics_store.rs   # メトリクス蓄積
│   │       └── report_sink/           # レポート出力先
│   │           ├── mod.rs
│   │           ├── file_writer.rs     # ファイル出力
│   │           └── email_sender.rs    # メール送信（モック）
│   │
│   ├── gui/                           # 🖥️ GUI（Serviceの出力を表示）
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── main.rs
│   │       ├── tui/                   # ターミナルUI (ratatui)
│   │       │   ├── mod.rs
│   │       │   ├── app.rs             # アプリケーション状態
│   │       │   ├── dashboard.rs       # ダッシュボード（service_gateway連携）
│   │       │   ├── portfolio_view.rs  # ポートフォリオ表示
│   │       │   ├── risk_view.rs       # リスクメトリクス表示
│   │       │   ├── trade_blotter.rs   # トレードブロッター
│   │       │   └── charts.rs          # チャート描画
│   │       └── web/                   # WebUI (service_gateway REST連携)
│   │           ├── mod.rs
│   │           ├── app.rs
│   │           ├── components/
│   │           └── api_client.rs      # REST API呼び出し
│   │
│   ├── frictional_bank/               # 🏦 デモオーケストレーター
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── main.rs                # デモエントリーポイント
│   │       ├── lib.rs
│   │       ├── orchestrator/          # ワークフロー制御
│   │       │   ├── mod.rs
│   │       │   ├── eod_batch.rs       # EODバッチ（全レイヤー統合）
│   │       │   ├── intraday.rs        # イントラデイ処理
│   │       │   └── stress_test.rs     # ストレステスト
│   │       └── config/                # デモ設定
│   │           ├── mod.rs
│   │           └── demo_config.toml
│   │
│   ├── data/                          # 📊 サンプルデータ（inputsが使用）
│   │   ├── input/                     # 入力データ（Adapter向け）
│   │   │   ├── trades/
│   │   │   │   ├── equity_book.csv
│   │   │   │   ├── rates_book.csv
│   │   │   │   ├── fx_book.csv
│   │   │   │   ├── irs_trade.xml
│   │   │   │   └── cds_trade.xml
│   │   │   ├── market_data/
│   │   │   │   ├── yield_curves/
│   │   │   │   ├── volatility/
│   │   │   │   ├── credit_spreads/
│   │   │   │   └── spot_rates/
│   │   │   └── counterparties/
│   │   │       ├── counterparties.csv
│   │   │       ├── netting_sets.csv
│   │   │       └── csa_agreements.csv
│   │   ├── config/                    # 設定データ（Infra向け）
│   │   │   ├── neutryx.toml
│   │   │   ├── calendars/
│   │   │   │   └── holidays_2026.csv
│   │   │   └── master/
│   │   │       └── currencies.csv
│   │   └── output/                    # 出力データ（Service結果）
│   │       ├── reports/
│   │       ├── settlements/
│   │       └── regulatory/
│   │
│   └── notebooks/                     # 📓 Jupyter (service_python経由)
│       ├── 01_pricing_demo.ipynb
│       ├── 02_calibration_demo.ipynb
│       ├── 03_risk_analysis.ipynb
│       ├── 04_xva_calculation.ipynb
│       └── 05_performance_bench.ipynb
│
├── docs/
│   └── demo/
│       ├── FRICTIONAL_BANK_SPEC.md    # 本ドキュメント
│       ├── DEMO_GUIDE.md
│       ├── DATA_FLOW.md               # A-I-P-Sフロー説明
│       └── SCENARIOS.md
```

## デモシナリオ

### 1️⃣ EODバッチ処理シナリオ

```text
[inputs]                              [Neutryx A-I-P-S]                    [outputs]
       │                                     │                                     │
       │  ① 取引データ生成                    │                                     │
       │  (fpml_generator)                   │                                     │
       ├────────────────────────────────────→│                                     │
       │                          ② adapter_fpml で受信                            │
       │                          ③ adapter_loader でCSV読込                       │
       │                                     │                                     │
       │  ④ マーケットデータ配信               │                                     │
       │  (bloomberg_sim)                    │                                     │
       ├────────────────────────────────────→│                                     │
       │                          ⑤ adapter_feeds で受信                           │
       │                                     │                                     │
       │                          ⑥ infra_config で設定読込                        │
       │                          ⑦ infra_master でカレンダー参照                  │
       │                                     │                                     │
       │                          ⑧ pricer_models で商品構築                       │
       │                          ⑨ pricer_optimiser でキャリブレーション           │
       │                          ⑩ pricer_pricing で価格計算                      │
       │                          ⑪ pricer_risk でXVA/Exposure計算                │
       │                                     │                                     │
       │                          ⑫ infra_store で結果永続化                       │
       │                                     │                                     │
       │                          ⑬ service_cli でレポート生成                     │
       │                          ⑭ service_gateway でAPI公開                     │
       │                                     │                                     │
       │                                     ├────────────────────────────────────→│
       │                                     │  ⑮ regulator_api へ規制報告         │
       │                                     │  ⑯ swift_receiver へ決済指図        │
       │                                     │  ⑰ risk_dashboard へメトリクス      │
       │                                     │                                     │
```

### 2️⃣ リアルタイムトレーディングシナリオ

```text
[inputs]                              [Neutryx A-I-P-S]                    [outputs]
       │                                     │                                     │
       │  ① ティッカーストリーム               │                                     │
       │  (reuters_sim)                      │                                     │
       ├───── WebSocket ────────────────────→│                                     │
       │                          ② adapter_feeds で受信                           │
       │                          ③ pricer_pricing でリアルタイム再評価            │
       │                          ④ pricer_risk でリスク更新                       │
       │                          ⑤ service_gateway でWebSocket配信               │
       │                                     ├───── WebSocket ───────────────────→│
       │                                     │  ⑥ websocket_sink で受信            │
       │                                     │  ⑦ GUI (TUI/Web) で表示             │
```

### 3️⃣ Python分析シナリオ

```text
[Jupyter Notebook]                   [Neutryx A-I-P-S]
       │                                     │
       │  ① Python関数呼び出し                │
       ├────────────────────────────────────→│
       │                          ② service_python で受信                         │
       │                          ③ pricer_* で計算実行                            │
       │                          ④ service_python で結果返却                      │
       │←────────────────────────────────────┤
       │  ⑤ DataFrame/Plotで可視化           │
```

## 機能配置マトリクス

| レイヤー | 役割 | デモでの使用 |
|----------|------|--------------|
| **inputs** | 外部入力元シミュレーション | Adapterにデータを供給 |
| **A: Adapter** | 外部データ受信・正規化 | inputsからデータ受信 |
| **I: Infra** | 設定・マスタ・永続化 | 横断的に利用 |
| **P: Pricer** | 計算処理 | コアロジック実行 |
| **S: Service** | 結果出力・API公開 | outputsへ配信 |
| **outputs** | 外部出力先シミュレーション | Serviceから結果受信 |
| **GUI** | 可視化 | service_gatewayのREST/WebSocket経由 |

## Cargo.toml設定

### ルートCargo.tomlへの追加

```toml
[workspace]
members = [
    # ... existing members ...
    
    # --- Demo ---
    "demo/inputs",
    "demo/outputs",
    "demo/frictional_bank",
    "demo/gui",
]
```

### demo/frictional_bank/Cargo.toml

```toml
[package]
name = "frictional_bank"
version = "0.1.0"
edition = "2021"
description = "FrictionalBank demo application showcasing Neutryx capabilities"

[dependencies]
# 全Neutryxクレートを統合
adapter_feeds = { path = "../../crates/adapter_feeds" }
adapter_fpml = { path = "../../crates/adapter_fpml" }
adapter_loader = { path = "../../crates/adapter_loader" }
infra_config = { path = "../../crates/infra_config" }
infra_master = { path = "../../crates/infra_master" }
infra_store = { path = "../../crates/infra_store" }
pricer_core = { path = "../../crates/pricer_core" }
pricer_models = { path = "../../crates/pricer_models" }
pricer_optimiser = { path = "../../crates/pricer_optimiser" }
pricer_risk = { path = "../../crates/pricer_risk" }

# デモ固有依存
demo_inputs = { path = "../inputs" }
demo_outputs = { path = "../outputs" }

# 共通依存
tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
```

### demo/inputs/Cargo.toml

```toml
[package]
name = "demo_inputs"
version = "0.1.0"
edition = "2021"
description = "Mock input systems for FrictionalBank demo"

[dependencies]
# Adapterが受け取る型を生成するため
pricer_core = { path = "../../crates/pricer_core" }
pricer_models = { path = "../../crates/pricer_models" }

tokio = { workspace = true }
rand = { workspace = true }
chrono = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
```

### demo/outputs/Cargo.toml

```toml
[package]
name = "demo_outputs"
version = "0.1.0"
edition = "2021"
description = "Mock output systems for FrictionalBank demo"

[dependencies]
# Serviceが出力する型を受け取るため
pricer_core = { path = "../../crates/pricer_core" }
pricer_risk = { path = "../../crates/pricer_risk" }

tokio = { workspace = true }
axum = { workspace = true }
tracing = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
```

### demo/gui/Cargo.toml

```toml
[package]
name = "demo_gui"
version = "0.1.0"
edition = "2021"
description = "GUI for FrictionalBank demo"

[dependencies]
# service_gatewayのAPIを呼び出す
reqwest = { version = "0.12", features = ["json"] }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

# TUI
ratatui = "0.29"
crossterm = "0.28"

[features]
default = ["tui"]
tui = []
web = []
```

## 推奨実装順序

1. **Phase 1**: `demo/data/` サンプルデータ整備
2. **Phase 2**: `demo/inputs/` 仮想入力システム実装
3. **Phase 3**: `demo/outputs/` 仮想出力システム実装
4. **Phase 4**: `demo/frictional_bank/` オーケストレーター実装
5. **Phase 5**: `demo/gui/tui/` ターミナルダッシュボード
6. **Phase 6**: `demo/notebooks/` Python連携デモ
7. **Phase 7**: `demo/gui/web/` Webダッシュボード（オプション）

## 設計原則

1. **A-I-P-S順序厳守**: データは必ずAdapter→Infra→Pricer→Serviceの順で流れる
2. **外部システム分離**: inputs/outputsは完全に独立したクレート
3. **Serviceが唯一の出口**: 外部への出力はすべてServiceレイヤー経由
4. **GUIはServiceのクライアント**: GUIはservice_gatewayのREST/WebSocket APIを利用
