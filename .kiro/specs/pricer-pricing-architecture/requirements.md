# Requirements Document

## Project Description (Input)
crates\pricer_pricing内の整理をする。まずpricerが中心にあり、pricer_modelsの機能を用いてPricingResultsを作る、が基本路線。その下部構造として設定ファイルを元にDiscountやMCやTreeでPriceが作られていくのがわかるように。ゼロベースで考えて。

## Requirements
<!-- Will be generated in /kiro:spec-requirements phase -->
