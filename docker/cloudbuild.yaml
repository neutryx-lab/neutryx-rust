steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'Dockerfile.gui'
      - '-t'
      - 'gcr.io/$PROJECT_ID/ergodic-bank:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/ergodic-bank:latest'
      - '.'

  # Push to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/ergodic-bank:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/ergodic-bank:latest'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:slim'
    args:
      - 'gcloud'
      - 'run'
      - 'deploy'
      - 'ergodic-bank'
      - '--image'
      - 'gcr.io/$PROJECT_ID/ergodic-bank:$COMMIT_SHA'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'

substitutions:
  _REGION: 'asia-northeast1'

options:
  logging: CLOUD_LOGGING_ONLY
